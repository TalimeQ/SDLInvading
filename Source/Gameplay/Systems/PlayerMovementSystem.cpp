#include "PlayerMovementSystem.h"
#include "../../Core/ECS/SceneView.h"

void PlayerControllerSystem::UpdateShipTransform(double DeltaTime, Scene& SceneRef)
{
    //for (EntityID ParsedEntity : SceneView<Transform, CShape>(scene))
    //{
    //    Transform* pTransform = scene.Get<Transform>(ent);
    //    Shape* pShape = scene.Get<Shape>(ent);

    //    // Do stuff
    //}

}

